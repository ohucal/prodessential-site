# SPEC: <task name>

**Purpose:** <one sentence — what this spec produces and why>

## Preconditions
- Read `CLAUDE.md`.
- <state, branch, or files that must exist first>

## Inputs
- <files, data, or params the task reads>

## Steps
1. <ordered, concrete steps>
2. <...>

## Output format
<exact shape of the deliverable — a file, a report, code changes. Show the structure.>

## Definition of done
- [ ] <objective, checkable criteria>
- [ ] `npx tsc --noEmit` passes (if code changed).
- [ ] Change verified in the preview (if UI changed).

## Guardrails
- <what NOT to touch; scope limits; when to stop and ask instead of guessing>
